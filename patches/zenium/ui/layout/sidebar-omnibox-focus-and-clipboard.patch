diff --git a/chrome/browser/ui/views/omnibox/omnibox_popup_view_full_webui.cc b/chrome/browser/ui/views/omnibox/omnibox_popup_view_full_webui.cc
--- a/chrome/browser/ui/views/omnibox/omnibox_popup_view_full_webui.cc
+++ b/chrome/browser/ui/views/omnibox/omnibox_popup_view_full_webui.cc
@@ -230,6 +230,13 @@
   if (presenter()) {
     presenter()->Hide();
   }
+  // Hiding the separately hosted popup can reactivate the browser Widget and
+  // restore its previously focused native location field. Put physical focus
+  // on the newly active page after that handoff; killing only the edit model's
+  // logical focus is not sufficient to prevent a later address-bar reopen.
+  controller()->client()->FocusWebContents();
+  controller()->edit_model()->OnKillFocus();
+
   // Push the restored state to the WebUI handler so it can render the
   // correct text and selection range for the newly selected tab.
   if (auto* popup_handler = GetPopupHandler()) {
diff --git a/components/omnibox/browser/autocomplete_classifier.cc b/components/omnibox/browser/autocomplete_classifier.cc
--- a/components/omnibox/browser/autocomplete_classifier.cc
+++ b/components/omnibox/browser/autocomplete_classifier.cc
@@ -58,6 +58,9 @@
       // Custom search engines cannot be used on mobile.
       AutocompleteProvider::TYPE_KEYWORD | AutocompleteProvider::TYPE_OPEN_TAB |
       AutocompleteProvider::TYPE_FEATURED_SEARCH |
+      // Zenium's floating omnibox offers a recently copied web address as its
+      // first zero-prefix action on desktop.
+      AutocompleteProvider::TYPE_CLIPBOARD |
       // Most visited sites for desktop.
       (omnibox_feature_configs::OmniboxUrlSuggestionsOnFocus::Get().enabled
            ? AutocompleteProvider::TYPE_MOST_VISITED_SITES
diff --git a/components/omnibox/browser/clipboard_provider.h b/components/omnibox/browser/clipboard_provider.h
--- a/components/omnibox/browser/clipboard_provider.h
+++ b/components/omnibox/browser/clipboard_provider.h
@@ -81,6 +81,13 @@
                                  base::TimeDelta clipboard_contents_age,
                                  std::set<ClipboardContentType> matched_types);
 
+  // Adds the desktop URL-only match after validating the clipboard text as an
+  // HTTP(S) address. Desktop clipboard metadata often reports copied URLs as
+  // plain text, so checking formats alone is insufficient.
+  void OnReceiveDesktopURL(const AutocompleteInput& input,
+                           base::TimeDelta clipboard_contents_age,
+                           std::optional<GURL> optional_url);
+
   // Checks whether the current template url supports text searches.
   bool TemplateURLSupportsTextSearch();
   // Checks whether the current template url supports image searches.
diff --git a/components/omnibox/browser/clipboard_provider.cc b/components/omnibox/browser/clipboard_provider.cc
--- a/components/omnibox/browser/clipboard_provider.cc
+++ b/components/omnibox/browser/clipboard_provider.cc
@@ -249,6 +249,18 @@
 }
 
 void ClipboardProvider::CheckClipboardContent(const AutocompleteInput& input) {
+  // Desktop clipboard implementations commonly expose a copied address as
+  // plain text rather than a URL format. Read and validate the value itself so
+  // Zenium can offer only genuine web addresses, never arbitrary copied text.
+#if !BUILDFLAG(IS_ANDROID) && !BUILDFLAG(IS_IOS)
+  done_ = false;
+  const base::TimeDelta clipboard_contents_age =
+      clipboard_content_->GetClipboardContentAge();
+  clipboard_content_->GetRecentURLFromClipboard(base::BindOnce(
+      &ClipboardProvider::OnReceiveDesktopURL,
+      callback_weak_ptr_factory_.GetWeakPtr(), input, clipboard_contents_age));
+  return;
+#else
   std::set<ClipboardContentType> desired_types;
   desired_types.insert(ClipboardContentType::URL);
 
@@ -273,6 +285,19 @@
       base::BindOnce(&ClipboardProvider::OnReceiveClipboardContent,
                      callback_weak_ptr_factory_.GetWeakPtr(), input,
                      clipboard_contents_age));
+#endif
+}
+
+void ClipboardProvider::OnReceiveDesktopURL(
+    const AutocompleteInput& input,
+    base::TimeDelta clipboard_contents_age,
+    std::optional<GURL> optional_url) {
+  if (optional_url && optional_url->SchemeIsHTTPOrHTTPS()) {
+    AddCreatedMatchWithTracking(
+        input, NewClipboardURLMatch(*optional_url), clipboard_contents_age);
+    NotifyListeners(true);
+  }
+  done_ = true;
 }
 
 void ClipboardProvider::OnReceiveClipboardContent(
diff --git a/components/omnibox/browser/autocomplete_grouper_sections.cc b/components/omnibox/browser/autocomplete_grouper_sections.cc
--- a/components/omnibox/browser/autocomplete_grouper_sections.cc
+++ b/components/omnibox/browser/autocomplete_grouper_sections.cc
@@ -419,6 +419,10 @@
           {
               Group(1,
                     {
+                        {omnibox::GROUP_MOBILE_CLIPBOARD, 1},
+                    }),
+              Group(1,
+                    {
                         {omnibox::GROUP_CROSS_DEVICE_TABS, 1},
                     }),
               Group(
@@ -503,6 +507,10 @@
           {
               Group(1,
                     {
+                        {omnibox::GROUP_MOBILE_CLIPBOARD, 1},
+                    }),
+              Group(1,
+                    {
                         {omnibox::GROUP_CROSS_DEVICE_TABS, 1},
                     }),
               Group(
@@ -531,6 +539,10 @@
     size_t limit)
     : ZpsSection(limit,
                  {
+                     Group(1,
+                           {
+                               {omnibox::GROUP_MOBILE_CLIPBOARD, 1},
+                           }),
                      Group(limit,
                            {
                                {omnibox::GROUP_MOST_VISITED, limit},
@@ -547,6 +559,10 @@
               {
                   Group(1,
                         {
+                            {omnibox::GROUP_MOBILE_CLIPBOARD, 1},
+                        }),
+                  Group(1,
+                        {
                             {omnibox::GROUP_CROSS_DEVICE_TABS, 1},
                         }),
                   Group(limit,
@@ -574,6 +590,10 @@
         size_t contextual_search_limit)
     : Section(contextual_action_limit + contextual_search_limit,
               {
+                  Group(1,
+                        {
+                            {omnibox::GROUP_MOBILE_CLIPBOARD, 1},
+                        }),
                   Group(contextual_action_limit,
                         {
                             {omnibox::GROUP_CONTEXTUAL_SEARCH_ACTION,
diff --git a/components/open_from_clipboard/clipboard_recent_content_generic.cc b/components/open_from_clipboard/clipboard_recent_content_generic.cc
--- a/components/open_from_clipboard/clipboard_recent_content_generic.cc
+++ b/components/open_from_clipboard/clipboard_recent_content_generic.cc
@@ -80,6 +80,12 @@
   if (!gurl_string.empty() &&
       gurl_string.find_first_of(base::kWhitespaceASCII) == std::string::npos) {
     url = GURL(gurl_string);
+    // Desktop address bars commonly copy their displayed, elided form without
+    // a scheme. Accept a host-like value while keeping ordinary copied words
+    // out of the URL-only clipboard suggestion.
+    if (!url.is_valid() && gurl_string.find('.') != std::string::npos) {
+      url = GURL("https://" + gurl_string);
+    }
   }
 
   OnReadURL(std::move(callback),
@@ -206,6 +212,12 @@
   const base::Time last_modified_time =
       ui::Clipboard::GetForCurrentThread()->GetLastModifiedTime();
   const base::Time now = base::Time::Now();
+  // Some desktop clipboard backends can read the current selection but cannot
+  // report its modification timestamp. Treat unknown age as current instead
+  // of suppressing a valid clipboard URL forever.
+  if (last_modified_time.is_null()) {
+    return base::TimeDelta();
+  }
   // In case of system clock change, assume the last modified time is now.
   // (Don't return a negative age, i.e., a time in the future.)
   if (last_modified_time > now)
diff --git a/components/omnibox/browser/clipboard_provider_unittest.cc b/components/omnibox/browser/clipboard_provider_unittest.cc
--- a/components/omnibox/browser/clipboard_provider_unittest.cc
+++ b/components/omnibox/browser/clipboard_provider_unittest.cc
@@ -183,7 +183,11 @@
 TEST_F(ClipboardProviderTest, DeleteMatch) {
   client_->set_template_url_service(
       search_engines_test_environment_.template_url_service());
+#if BUILDFLAG(IS_ANDROID) || BUILDFLAG(IS_IOS)
   SetClipboardText(kClipboardText);
+#else
+  SetClipboardUrl(GURL(kClipboardURL));
+#endif
   provider_->Start(
       CreateAutocompleteInput(metrics::OmniboxFocusType::INTERACTION_FOCUS),
       false);
@@ -205,9 +209,17 @@
   EXPECT_EQ(AutocompleteMatchType::CLIPBOARD_URL,
             provider_->matches().back().type);
 
+  // Mobile exposes a metadata-only row until the user chooses it. Desktop
+  // validates and populates URL clipboard rows before displaying them.
+#if BUILDFLAG(IS_ANDROID) || BUILDFLAG(IS_IOS)
   EXPECT_TRUE(provider_->matches().back().destination_url.is_empty());
+#else
+  EXPECT_EQ(GURL(kClipboardURL),
+            provider_->matches().back().destination_url);
+#endif
 }
 
+#if BUILDFLAG(IS_ANDROID) || BUILDFLAG(IS_IOS)
 TEST_F(ClipboardProviderTest, CreateBlankTextMatchOnStart) {
   client_->set_template_url_service(
       search_engines_test_environment_.template_url_service());
@@ -238,6 +250,28 @@
             provider_->matches().back().type);
   EXPECT_FALSE(provider_->matches().back().post_content.get());
 }
+#else
+TEST_F(ClipboardProviderTest, DesktopIgnoresCopiedTextOnStart) {
+  client_->set_template_url_service(
+      search_engines_test_environment_.template_url_service());
+  SetClipboardText(kClipboardText);
+  provider_->Start(
+      CreateAutocompleteInput(metrics::OmniboxFocusType::INTERACTION_FOCUS),
+      false);
+  EXPECT_TRUE(provider_->matches().empty());
+}
+
+TEST_F(ClipboardProviderTest, DesktopIgnoresCopiedImageOnStart) {
+  client_->set_template_url_service(
+      search_engines_test_environment_.template_url_service());
+  SetClipboardImage(gfx::test::CreateImage(/*width=*/10, /*height=*/10));
+  provider_->Start(
+      CreateAutocompleteInput(metrics::OmniboxFocusType::INTERACTION_FOCUS),
+      false);
+  EXPECT_TRUE(provider_->matches().empty());
+}
+#endif
+
 TEST_F(ClipboardProviderTest, SkipImageMatchGivenWantAsynchronousMatchesFalse) {
   client_->set_template_url_service(
       search_engines_test_environment_.template_url_service());
diff --git a/components/omnibox/browser/autocomplete_grouper_sections_unittest.cc b/components/omnibox/browser/autocomplete_grouper_sections_unittest.cc
--- a/components/omnibox/browser/autocomplete_grouper_sections_unittest.cc
+++ b/components/omnibox/browser/autocomplete_grouper_sections_unittest.cc
@@ -2115,6 +2115,7 @@
                  std::vector<int> expected_relevances) {
     PSections sections;
     omnibox::GroupConfigMap group_configs;
+    group_configs[omnibox::GROUP_MOBILE_CLIPBOARD];
     group_configs[omnibox::GROUP_MOST_VISITED];
     group_configs[omnibox::GROUP_PREVIOUS_SEARCH_RELATED];
     // Max 8 suggestions, with an upper limit of 4 search suggestions.
@@ -2124,6 +2125,16 @@
     VerifyMatches(out_matches, expected_relevances);
   };
   {
+    SCOPED_TRACE("Clipboard URL is the first desktop zero-prefix suggestion.");
+    test(0,
+         {
+             CreateMatch(101, omnibox::GROUP_MOBILE_CLIPBOARD),
+             CreateMatch(100, omnibox::GROUP_PREVIOUS_SEARCH_RELATED),
+             CreateMatch(99, omnibox::GROUP_MOST_VISITED),
+         },
+         {101, 100, 99});
+  }
+  {
     SCOPED_TRACE(
         "Given 12 srp zps matches, the group should respect the search "
         "suggestion limit");
