Index: src/chrome/app/chromium_strings.grd
===================================================================
--- src.orig/chrome/app/chromium_strings.grd
+++ src/chrome/app/chromium_strings.grd
@@ -1069,6 +1069,10 @@ Chromium is unable to recover your setti
         Customize and control Chromium. Update is available.
       </message>
 
+      <message name="IDS_APPMENU_TOOLTIP_ZENIUM_SERVICES_UPDATE" desc="The tooltip to show for the browser menu when Zenium services schema changes">
+        Zenium services have been updated. Please review the changes.
+      </message>
+
       <message name="IDS_APPMENU_TOOLTIP_ALERT" desc="The tooltip to show for the browser menu when a non-update alert is displayed">
         Customize and control Chromium. Something needs your attention - click for details.
       </message>
@@ -1806,6 +1810,11 @@ Chromium is unable to recover your setti
         Chromium couldn't update to the latest version, so you're missing out on new features and security fixes.
       </message>
 
+      <!-- Zenium services schema change bubble -->
+      <message name="IDS_ZENIUM_SERVICES_SCHEMA_MENU_ITEM" desc="Text for the bubble that notifies the user of a schema change in Zenium services.">
+        Review Zenium services updates
+      </message>
+
       <!-- User menu errors -->
       <message name="IDS_SYNC_ERROR_USER_MENU_UPGRADE_BUTTON" desc="Button in the header of desktop user menu that prompts the user to update Chrome to fix the out-of-date Chrome client error.">
         Update Chromium
Index: src/chrome/app/generated_resources.grd
===================================================================
--- src.orig/chrome/app/generated_resources.grd
+++ src/chrome/app/generated_resources.grd
@@ -13414,6 +13414,9 @@ Check your passwords anytime in <ph name
       <message name="IDS_APP_MENU_BUTTON_UPDATE" desc="Short label next to app-menu button when an update is available.">
         Update
       </message>
+      <message name="IDS_APP_MENU_BUTTON_ZENIUM_SERVICES_UPDATE" desc="Short label next to app-menu button when Zenium services behavior changes.">
+        Services updated
+      </message>
       <if expr="is_win or is_macosx or is_linux">
         <message name="IDS_APP_MENU_BUTTON_UPDATE_ALT1" desc="Alternate short label next to app-menu button when an update is available.">
           Finish update
Index: src/chrome/app/chrome_command_ids.h
===================================================================
--- src.orig/chrome/app/chrome_command_ids.h
+++ src/chrome/app/chrome_command_ids.h
@@ -314,6 +314,8 @@
 #define IDC_SHOW_TABS_FROM_OTHER_DEVICES_SIDE_PANEL 40304
 #define IDC_CHROME_ENTERPRISE_RELEASE_NOTES 40305
 
+#define IDC_ZENIUM_SERVICES_OPEN 40700
+
 // Spell-check
 // Insert any additional suggestions before _LAST; these have to be consecutive.
 #define IDC_SPELLCHECK_SUGGESTION_0     41000
Index: src/chrome/browser/ui/chrome_pages.h
===================================================================
--- src.orig/chrome/browser/ui/chrome_pages.h
+++ src/chrome/browser/ui/chrome_pages.h
@@ -141,6 +141,7 @@ void ShowSafeBrowsingEnhancedProtectionW
     safe_browsing::SafeBrowsingSettingReferralMethod referral_method);
 void ShowImportDialog(BrowserWindowInterface* browser);
 void ShowAboutChrome(BrowserWindowInterface* browser);
+void ShowZeniumServices(BrowserWindowInterface* browser);
 void ShowSearchEngineSettings(BrowserWindowInterface* browser);
 void ShowWebStore(BrowserWindowInterface* browser,
                   std::string_view utm_source_value);
Index: src/chrome/browser/ui/chrome_pages.cc
===================================================================
--- src.orig/chrome/browser/ui/chrome_pages.cc
+++ src/chrome/browser/ui/chrome_pages.cc
@@ -618,6 +618,11 @@ void ShowAboutChrome(BrowserWindowInterf
   ShowSingletonTabIgnorePathOverwriteNTP(browser, GURL(kChromeUIHelpURL));
 }
 
+void ShowZeniumServices(BrowserWindowInterface* browser) {
+  ShowSingletonTabIgnorePathOverwriteNTP(
+      browser, GURL("chrome://settings/privacy/services"));
+}
+
 void ShowSearchEngineSettings(BrowserWindowInterface* browser) {
   base::RecordAction(UserMetricsAction("EditSearchEngines"));
   ShowSettingsSubPage(
Index: src/chrome/browser/ui/browser_command_controller.cc
===================================================================
--- src.orig/chrome/browser/ui/browser_command_controller.cc
+++ src/chrome/browser/ui/browser_command_controller.cc
@@ -1287,6 +1287,9 @@ void BrowserCommandController::HandleCom
     case IDC_UPGRADE_DIALOG:
       OpenUpdateChromeDialog(browser_);
       break;
+    case IDC_ZENIUM_SERVICES_OPEN:
+      ShowZeniumServices(browser_->GetBrowserForOpeningWebUi());
+      break;
     case IDC_OPEN_SAFETY_HUB:
       ShowSettingsSubPage(browser_->GetBrowserForOpeningWebUi(),
                           chrome::kSafetyHubSubPage);
@@ -1933,6 +1936,7 @@ void BrowserCommandController::InitComma
 
   // These are always enabled; the menu determines their menu item visibility.
   command_updater_->UpdateCommandEnabled(IDC_UPGRADE_DIALOG, true);
+  command_updater_->UpdateCommandEnabled(IDC_ZENIUM_SERVICES_OPEN, true);
   command_updater_->UpdateCommandEnabled(IDC_SET_BROWSER_AS_DEFAULT, true);
 
   // Safety Hub commands.
Index: src/chrome/browser/ui/toolbar/app_menu_model.h
===================================================================
--- src.orig/chrome/browser/ui/toolbar/app_menu_model.h
+++ src/chrome/browser/ui/toolbar/app_menu_model.h
@@ -313,6 +313,9 @@ class AppMenuModel : public ui::SimpleMe
   // boolean indicating whether any menu items were added.
   bool AddDefaultBrowserMenuItems();
 
+  // Adds a nag to review Zenium services permission changes
+  bool AddZeniumSchemaItem();
+
   // Adds the Safety Hub menu notifications to the menu. Returns a boolean
   // indicating whether any menu items were added.
   [[nodiscard]] bool AddSafetyHubMenuItem();
Index: src/chrome/browser/ui/toolbar/app_menu_model.cc
===================================================================
--- src.orig/chrome/browser/ui/toolbar/app_menu_model.cc
+++ src/chrome/browser/ui/toolbar/app_menu_model.cc
@@ -115,6 +115,7 @@
 #include "components/dom_distiller/core/url_utils.h"
 #include "components/feature_engagement/public/event_constants.h"
 #include "components/feature_engagement/public/feature_constants.h"
+#include "components/zenium_services/schema.h"
 #include "components/lens/lens_features.h"
 #include "components/omnibox/browser/vector_icons.h"
 #include "components/password_manager/content/common/web_ui_constants.h"
@@ -2260,7 +2261,8 @@ bool AppMenuModel::AddGlobalErrorMenuIte
   // how update the menu if new errors are added.
   const GlobalErrorService::GlobalErrorList& errors =
       GlobalErrorServiceFactory::GetForProfile(browser_->profile())->errors();
-  bool menu_items_added = false;
+  bool menu_items_added = AddZeniumSchemaItem();
+
   for (GlobalError* error : errors) {
     DCHECK(error);
     if (error->HasMenuItem()) {
@@ -2327,6 +2329,22 @@ bool AppMenuModel::AddSafetyHubMenuItem(
   return true;
 }
 
+bool AppMenuModel::AddZeniumSchemaItem() {
+  if (!zenium::ShouldShowSchemaNotification(
+          *browser()->profile()->GetPrefs())) {
+    return false;
+  }
+
+  const auto update_icon = ui::ImageModel::FromVectorIcon(
+      omnibox::kProductChromeRefreshOldIcon, ui::kColorMenuIcon, kDefaultIconSize);
+  AddItemWithIcon(
+      IDC_ZENIUM_SERVICES_OPEN,
+      l10n_util::GetStringUTF16(IDS_ZENIUM_SERVICES_SCHEMA_MENU_ITEM),
+      update_icon);
+
+  return true;
+}
+
 #if BUILDFLAG(IS_CHROMEOS)
 void AppMenuModel::UpdateSettingsItemState() {
   bool is_disabled =
Index: src/chrome/browser/ui/toolbar/app_menu_icon_controller.cc
===================================================================
--- src.orig/chrome/browser/ui/toolbar/app_menu_icon_controller.cc
+++ src/chrome/browser/ui/toolbar/app_menu_icon_controller.cc
@@ -9,11 +9,15 @@
 #include "build/branding_buildflags.h"
 #include "build/build_config.h"
 #include "chrome/browser/defaults.h"
+#include "chrome/browser/profiles/profile.h"
 #include "chrome/browser/ui/global_error/global_error.h"
 #include "chrome/browser/ui/global_error/global_error_service_factory.h"
 #include "chrome/browser/ui/ui_features.h"
 #include "chrome/browser/upgrade_detector/upgrade_detector.h"
 #include "chrome/common/channel_info.h"
+#include "components/zenium_services/pref_names.h"
+#include "components/zenium_services/schema.h"
+#include "components/prefs/pref_change_registrar.h"
 #include "chrome/grit/branded_strings.h"
 #include "chrome/grit/generated_resources.h"
 #include "components/version_info/channel.h"
@@ -114,6 +118,14 @@ AppMenuIconController::AppMenuIconContro
       GlobalErrorServiceFactory::GetForProfile(profile_));
 
   upgrade_detector_->AddObserver(this);
+
+  if (auto* prefs = profile->GetPrefs()) {
+    registrar_.Init(prefs);
+    registrar_.Add(
+        prefs::kZeniumSchemaVersion,
+        base::BindRepeating(&AppMenuIconController::OnGlobalErrorsChanged,
+                            base::Unretained(this)));
+  }
 }
 
 AppMenuIconController::~AppMenuIconController() {
@@ -146,6 +158,12 @@ AppMenuIconController::GetTypeAndSeverit
                         ->GetHighestSeverityGlobalErrorWithAppMenuItem()) {
     return {IconType::kGlobalError, SeverityFromError(error)};
   }
+
+  if (auto* prefs = profile_->GetPrefs()) {
+    if (zenium::ShouldShowSchemaNotification(*prefs)) {
+      return {IconType::kZeniumServicesUpdate, Severity::kLow};
+    }
+  }
 #endif
 
   return {IconType::kNone, Severity::kNone};
@@ -182,6 +200,9 @@ std::u16string AppMenuIconController::Ge
 #else
     return l10n_util::GetStringUTF16(IDS_APP_MENU_BUTTON_UPDATE);
 #endif
+  } else if (type == IconType::kZeniumServicesUpdate) {
+    return l10n_util::GetStringUTF16(
+        IDS_APP_MENU_BUTTON_ZENIUM_SERVICES_UPDATE);
   } else {
     const int text_id = severity == Severity::kLow
                             ? IDS_APP_MENU_BUTTON_ACTION_REQUIRED
@@ -207,6 +228,8 @@ std::u16string AppMenuIconController::Ge
     return l10n_util::GetStringUTF16(IDS_APPMENU_TOOLTIP);
   } else if (type == IconType::kUpgradeNotification) {
     return l10n_util::GetStringUTF16(IDS_APPMENU_TOOLTIP_UPDATE_AVAILABLE);
+  } else if (type == IconType::kZeniumServicesUpdate) {
+    return l10n_util::GetStringUTF16(IDS_APPMENU_TOOLTIP_ZENIUM_SERVICES_UPDATE);
   } else {
     return l10n_util::GetStringUTF16(IDS_APPMENU_TOOLTIP_ALERT);
   }
Index: src/chrome/browser/ui/toolbar/app_menu_icon_controller.h
===================================================================
--- src.orig/chrome/browser/ui/toolbar/app_menu_icon_controller.h
+++ src/chrome/browser/ui/toolbar/app_menu_icon_controller.h
@@ -16,6 +16,7 @@
 #include "chrome/browser/ui/global_error/global_error_observer.h"
 #include "chrome/browser/ui/global_error/global_error_service.h"
 #include "chrome/browser/upgrade_detector/upgrade_observer.h"
+#include "components/prefs/pref_change_registrar.h"
 #include "third_party/skia/include/core/SkColor.h"
 #include "ui/base/models/image_model.h"
 
@@ -30,6 +31,7 @@ class AppMenuIconController : public Glo
   enum class IconType {
     kNone,
     kUpgradeNotification,
+    kZeniumServicesUpdate,
     kGlobalError,
   };
   enum class Severity {
@@ -106,6 +108,7 @@ class AppMenuIconController : public Glo
   const raw_ptr<Delegate> delegate_;
   base::ScopedObservation<GlobalErrorService, GlobalErrorObserver>
       global_error_observation_{this};
+  PrefChangeRegistrar registrar_;
 };
 
 #endif  // CHROME_BROWSER_UI_TOOLBAR_APP_MENU_ICON_CONTROLLER_H_
