--- a/chrome/browser/zenium_flag_entries.h
+++ b/chrome/browser/zenium_flag_entries.h
@@ -70,6 +70,10 @@
      "middleboxes and TLS endpoints mishandle this behavior and may become "
      "inaccessible. Zenium flag.",
      kOsAll, FEATURE_VALUE_TYPE(net::features::kZeniumSplitClientHello)},
+    {"no-proxy-server", "Ignore system proxy",
+     "Ignores system proxy settings and always connects directly. This may "
+     "prevent access to networks that require a proxy. Zenium flag.",
+     kOsAll, SINGLE_VALUE_TYPE(switches::kNoProxyServer)},
     // ui
     // platform
 #endif  // CHROME_BROWSER_ZENIUM_FLAG_ENTRIES_H_
