Index: src/build/apple/tweak_info_plist.py
===================================================================
--- src.orig/build/apple/tweak_info_plist.py
+++ src/build/apple/tweak_info_plist.py
@@ -116,7 +116,7 @@ def _AddVersionKeys(plist, version_forma
     VERSION_FILE = os.path.join(TOP, 'chrome/VERSION')
     (stdout, retval) = _GetOutput([
         VERSION_TOOL, '-f', VERSION_FILE, '-t',
-        '@MAJOR@.@MINOR@.@BUILD@.@PATCH@'
+        '@ZENIUM_MAJOR@.@ZENIUM_MINOR@.@ZENIUM_PATCH@.@ZENIUM_PLATFORM@'
     ])
 
     # If the command finished with a non-zero return code, then report the
@@ -423,7 +423,7 @@ def Main(argv):
         # http://lists.apple.com/archives/carbon-dev/2006/Jun/msg00139.html
         # BUILD will always be an increasing value, so BUILD_PATH gives us
         # something unique that meetings what LS wants.
-        'CFBundleVersion': '@BUILD@.@PATCH@',
+        'CFBundleVersion': '@MAJOR@.@MINOR@.@BUILD@.@PATCH@',
     }
   else:
     version_format_for_key = {
Index: src/base/version_info/version_info_values.h.version
===================================================================
--- src.orig/base/version_info/version_info_values.h.version
+++ src/base/version_info/version_info_values.h.version
@@ -7,6 +7,7 @@
 
 #define PRODUCT_NAME "@PRODUCT_FULLNAME@"
 #define PRODUCT_VERSION "@MAJOR@.@MINOR@.@BUILD@.@PATCH@"
+#define ZENIUM_PRODUCT_VERSION "@ZENIUM_MAJOR@.@ZENIUM_MINOR@.@ZENIUM_PATCH@.@ZENIUM_PLATFORM@"
 #define LAST_CHANGE "@LASTCHANGE@"
 #define IS_OFFICIAL_BUILD @OFFICIAL_BUILD@
 
Index: src/base/version_info/version_info.h
===================================================================
--- src.orig/base/version_info/version_info.h
+++ src/base/version_info/version_info.h
@@ -25,6 +25,10 @@ constexpr std::string_view GetProductNam
   return PRODUCT_NAME;
 }
 
+constexpr std::string_view GetZeniumVersionNumber() {
+  return ZENIUM_PRODUCT_VERSION;
+}
+
 // Returns the version number, e.g. "6.0.490.1".
 constexpr std::string_view GetVersionNumber() {
   return PRODUCT_VERSION;
Index: src/chrome/browser/ui/webui/version/version_ui.cc
===================================================================
--- src.orig/chrome/browser/ui/webui/version/version_ui.cc
+++ src/chrome/browser/ui/webui/version/version_ui.cc
@@ -247,6 +247,8 @@ void VersionUI::AddVersionDetailStrings(
                                   VersionProcessorVariation());
 
   // Data strings.
+  html_source->AddString(version_ui::kZeniumVersion,
+                         version_info::GetZeniumVersionNumber());
   html_source->AddString(version_ui::kVersion,
                          version_info::GetVersionNumber());
   html_source->AddString(version_ui::kVersionSuffix,
@@ -340,12 +342,14 @@ void VersionUI::AddVersionDetailStrings(
 std::u16string VersionUI::GetAnnotatedVersionStringForUi() {
   return l10n_util::GetStringFUTF16(
       IDS_SETTINGS_ABOUT_PAGE_BROWSER_VERSION,
+      {base::UTF8ToUTF16(version_info::GetZeniumVersionNumber()),
       base::UTF8ToUTF16(version_info::GetVersionNumber()),
       base::UTF8ToUTF16(GetVersionInformationalSuffix()),
       l10n_util::GetStringUTF16(version_info::IsOfficialBuild()
                                     ? IDS_VERSION_UI_OFFICIAL
                                     : IDS_VERSION_UI_UNOFFICIAL),
       base::UTF8ToUTF16(GetProductModifier()),
-      l10n_util::GetStringUTF16(VersionUI::VersionProcessorVariation()));
+      l10n_util::GetStringUTF16(VersionUI::VersionProcessorVariation()),
+      u"Chromium"}, nullptr);
 }
 #endif  // !BUILDFLAG(IS_ANDROID)
Index: src/components/webui/version/version_ui_constants.cc
===================================================================
--- src.orig/components/webui/version/version_ui_constants.cc
+++ src/components/webui/version/version_ui_constants.cc
@@ -88,6 +88,7 @@ const char kVariationsSourceName[] = "va
 const char kVariationsSeed[] = "variations_seed";
 const char kVariationsSeedName[] = "variations_seed_name";
 const char kVersion[] = "version";
+const char kZeniumVersion[] = "zenium_version";
 const char kVersionSuffix[] = "version_suffix";
 const char kVersionModifier[] = "version_modifier";
 const char kVersionProcessorVariation[] = "version_processor_variation";
Index: src/components/webui/version/version_ui_constants.h
===================================================================
--- src.orig/components/webui/version/version_ui_constants.h
+++ src/components/webui/version/version_ui_constants.h
@@ -92,6 +92,7 @@ extern const char kVariationsSourceName[
 extern const char kVariationsSeed[];
 extern const char kVariationsSeedName[];
 extern const char kVersion[];
+extern const char kZeniumVersion[];
 extern const char kVersionSuffix[];
 extern const char kVersionModifier[];
 extern const char kVersionProcessorVariation[];
Index: src/chrome/app/settings_strings.grdp
===================================================================
--- src.orig/chrome/app/settings_strings.grdp
+++ src/chrome/app/settings_strings.grdp
@@ -41,7 +41,7 @@
 
     <!-- About Page -->
     <message name="IDS_SETTINGS_ABOUT_PAGE_BROWSER_VERSION" desc="The text label describing the version of the browser, example: Version 57.0.2937.0-r123456 (Developer Build) unknown (64-bit). The suffix of the version (eg. '-r123456') exists only in limited cases.)">
-      Version <ph name="PRODUCT_VERSION">$1<ex>15.0.865.0</ex></ph><ph name="PRODUCT_VERSION_SUFFIX">$2<ex>-r123456</ex></ph>  (<ph name="PRODUCT_CHANNEL">$3<ex>Developer Build</ex></ph>) <ph name="PRODUCT_MODIFIER">$4</ph> <ph name="PRODUCT_VERSION_BITS">$5</ph>
+      Version <ph name="ZENIUM_PRODUCT_VERSION">$1<ex>0.0.0.0</ex></ph> (<ph name="PRODUCT_CHANNEL">$4<ex>Developer Build</ex></ph>, <ph name="CHROMIUM_NAME">$7</ph> <ph name="PRODUCT_VERSION">$2<ex>15.0.865.0</ex></ph><ph name="PRODUCT_VERSION_SUFFIX">$3<ex>-r123456</ex></ph>) <ph name="PRODUCT_MODIFIER">$5</ph> <ph name="PRODUCT_VERSION_BITS">$6</ph>
     </message>
     <message name="IDS_SETTINGS_ABOUT_PAGE_LEARN_MORE_UPDATE_ERRORS" desc="The accessibility label for a 'Learn more' link next to an error message about how an update has failed.">
       Learn more about fixing update errors
Index: src/components/webui/version/resources/about_version.html
===================================================================
--- src.orig/components/webui/version/resources/about_version.html
+++ src/components/webui/version/resources/about_version.html
@@ -54,11 +54,21 @@ about:version template page
       <table id="inner" cellpadding="0" cellspacing="0" border="0">
         <tr><td class="label">$i18n{application_label}</td>
           <td class="version" id="version">
+            <span id="copy-content-zenium">
+              <span>$i18n{zenium_version}</span>
+              (<span>$i18n{official}</span>)
+              <span>$i18n{version_processor_variation}</span>
+            </span>
+            <button id="copy-to-clipboard-zenium" aria-label="$i18n{copy_label}">
+              <div id="copy-to-clipboard-icon"></div>
+            </button>
+          </td>
+        </tr>
+        <tr><td class="label">Chromium</td>
+          <td class="version" id="version">
             <span id="copy-content">
               <span>$i18n{version}$i18n{version_suffix}</span>
-              (<span>$i18n{official}</span>)
               <span>$i18n{version_modifier}</span>
-              <span>$i18n{version_processor_variation}</span>
   <if expr="is_win">
               <span>$i18n{update_cohort_name}</span>
   </if>
Index: src/components/webui/version/resources/about_version.ts
===================================================================
--- src.orig/components/webui/version/resources/about_version.ts
+++ src/components/webui/version/resources/about_version.ts
@@ -134,6 +134,12 @@ async function copyVersionToClipboard()
   announceCopy('copy_notice');
 }
 
+async function copyZeniumVersionToClipboard() {
+  await navigator.clipboard.writeText(
+      getRequiredElement('copy-content-zenium').innerText);
+  announceCopy('copy_notice');
+}
+
 async function copyVariationsToClipboard() {
   const cmdLine =
       getRequiredElement('variations-cmd').dataset['value'] as string;
@@ -202,6 +208,9 @@ function initialize() {
   getRequiredElement('copy-to-clipboard')
       .addEventListener('click', copyVersionToClipboard);
 
+  getRequiredElement('copy-to-clipboard-zenium')
+      .addEventListener('click', copyZeniumVersionToClipboard);
+
   getRequiredElement('copy-variations-to-clipboard')
       .addEventListener('click', copyVariationsToClipboard);
 }
Index: src/components/webui/version/resources/about_version.css
===================================================================
--- src.orig/components/webui/version/resources/about_version.css
+++ src/components/webui/version/resources/about_version.css
@@ -90,6 +90,7 @@ body {
   vertical-align: bottom;
 }
 
+#copy-to-clipboard-zenium,
 #copy-to-clipboard,
 #copy-variations-to-clipboard {
   background-color: var(--background-color);
Index: src/chrome/app/chrome_main_delegate.cc
===================================================================
--- src.orig/chrome/app/chrome_main_delegate.cc
+++ src/chrome/app/chrome_main_delegate.cc
@@ -372,16 +372,16 @@ bool HandleCreditsSwitch(const base::Com
 bool HandleVersionSwitches(const base::CommandLine& command_line) {
 #if !BUILDFLAG(IS_MAC)
   if (command_line.HasSwitch(switches::kProductVersion)) {
-    UNSAFE_TODO(printf("%s\n", version_info::GetVersionNumber().data()));
+    UNSAFE_TODO(printf("%s\n", version_info::GetZeniumVersionNumber().data()));
     return true;
   }
 #endif
 
   if (command_line.HasSwitch(switches::kVersion)) {
     UNSAFE_TODO(printf(
-        "%s %s %s\n", version_info::GetProductName().data(),
-        version_info::GetVersionNumber().data(),
-        chrome::GetChannelName(chrome::WithExtendedStable(true)).c_str()));
+        "%s %s (Chromium %s)\n", version_info::GetProductName().data(),
+        version_info::GetZeniumVersionNumber().data(),
+        version_info::GetVersionNumber().data()));
     return true;
   }
 
Index: src/chrome/app/chrome_crash_reporter_client.cc
===================================================================
--- src.orig/chrome/app/chrome_crash_reporter_client.cc
+++ src/chrome/app/chrome_crash_reporter_client.cc
@@ -14,6 +14,7 @@
 #include "base/path_service.h"
 #include "base/strings/string_split.h"
 #include "base/strings/utf_string_conversions.h"
+#include "base/version_info/version_info.h"
 #include "build/branding_buildflags.h"
 #include "build/build_config.h"
 #include "chrome/common/channel_info.h"
@@ -130,7 +131,7 @@ bool ChromeCrashReporterClient::GetCrash
 void ChromeCrashReporterClient::GetProductInfo(ProductInfo* product_info) {
   CHECK(product_info);
   product_info->product_name = "Zenium";
-  product_info->version = UpdaterVersion();
+  product_info->version = version_info::GetZeniumVersionNumber();
   product_info->channel =
       chrome::GetChannelName(chrome::WithExtendedStable(true));
 }
Index: src/components/crash/core/app/crashpad_win.cc
===================================================================
--- src.orig/components/crash/core/app/crashpad_win.cc
+++ src/components/crash/core/app/crashpad_win.cc
@@ -16,6 +16,7 @@
 #include "base/strings/string_split.h"
 #include "base/strings/string_util.h"
 #include "base/strings/utf_string_conversions.h"
+#include "base/version_info/version_info.h"
 #include "base/win/windows_version.h"
 #include "build/branding_buildflags.h"
 #include "build/build_config.h"
@@ -39,7 +40,7 @@ void GetPlatformCrashpadAnnotations(
   crash_reporter_client->GetProductNameAndVersion(
       exe_file, &product_name, &version, &special_build, &channel_name);
   (*annotations)["prod"] = base::WideToUTF8(product_name);
-  (*annotations)["ver"] = base::WideToUTF8(version);
+  (*annotations)["ver"] = version_info::GetZeniumVersionNumber();
 #if BUILDFLAG(GOOGLE_CHROME_BRANDING)
   // Empty means stable.
   const bool allow_empty_channel = true;
Index: src/components/crash/core/app/crashpad_mac.mm
===================================================================
--- src.orig/components/crash/core/app/crashpad_mac.mm
+++ src/components/crash/core/app/crashpad_mac.mm
@@ -19,6 +19,7 @@
 #include "base/files/file_path.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/sys_string_conversions.h"
+#include "base/version_info/version_info.h"
 #include "build/branding_buildflags.h"
 #include "components/crash/core/app/crash_reporter_client.h"
 #include "third_party/crashpad/crashpad/client/crash_report_database.h"
@@ -77,10 +78,7 @@ std::map<std::string, std::string> GetPr
         }
       }
 
-      NSString* version =
-          base::apple::ObjCCast<NSString>([base::apple::FrameworkBundle()
-              objectForInfoDictionaryKey:@"CFBundleShortVersionString"]);
-      process_annotations["ver"] = base::SysNSStringToUTF8(version);
+      process_annotations["ver"] = version_info::GetZeniumVersionNumber();
 
       process_annotations["plat"] = std::string("OS X");
     }  // @autoreleasepool
