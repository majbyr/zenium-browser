Index: src/chrome/browser/zenium_flag_entries.h
===================================================================
--- src.orig/chrome/browser/zenium_flag_entries.h
+++ src/chrome/browser/zenium_flag_entries.h
@@ -12,6 +12,10 @@
      "Configures the frame rate the browser is throttled to when Energy Saver "
      "is enabled. Zenium flag.",
      kOsDesktop, MULTI_VALUE_TYPE(zenium::kEnergySaverFrameRateChoices)},
+    {"bookmark-open-all-confirmation", "Bookmark open-all confirmation",
+     "Controls whether Zenium asks for confirmation before opening 15 or more "
+     "bookmarks at once. Zenium flag.",
+     kOsDesktop, FEATURE_VALUE_TYPE(features::kBookmarkOpenAllConfirmation)},
     // core/network
     // ui
     // platform
Index: src/chrome/browser/ui/ui_features.cc
===================================================================
--- src.orig/chrome/browser/ui/ui_features.cc
+++ src/chrome/browser/ui/ui_features.cc
@@ -29,6 +29,9 @@ bool IsProcessOverheadExperimentActive()
 
 namespace features {
 
+// Controls the confirmation shown before opening many bookmarks at once.
+BASE_FEATURE(kBookmarkOpenAllConfirmation, base::FEATURE_ENABLED_BY_DEFAULT);
+
 // Enables the migration of browser commands to Action API.
 BASE_FEATURE(kUseActionsForBrowserCommands, base::FEATURE_DISABLED_BY_DEFAULT);
 
Index: src/chrome/browser/ui/ui_features.h
===================================================================
--- src.orig/chrome/browser/ui/ui_features.h
+++ src/chrome/browser/ui/ui_features.h
@@ -19,6 +19,8 @@ namespace features {
 // All features in alphabetical order. The features should be documented
 // alongside the definition of their values in the .cc file.
 
+BASE_DECLARE_FEATURE(kBookmarkOpenAllConfirmation);
+
 BASE_DECLARE_FEATURE(kUseActionsForBrowserCommands);
 
 BASE_DECLARE_FEATURE(kAllowEyeDropperWGCScreenCapture);
Index: src/chrome/browser/ui/bookmarks/bookmark_utils_desktop.cc
===================================================================
--- src.orig/chrome/browser/ui/bookmarks/bookmark_utils_desktop.cc
+++ src/chrome/browser/ui/bookmarks/bookmark_utils_desktop.cc
@@ -597,7 +597,8 @@ void OpenAllIfAllowed(
 
   // Skip the prompt if there are few bookmarks.
   size_t child_count = url_and_ids.size();
-  if (child_count < bookmarks::kNumBookmarkUrlsBeforePrompting) {
+  if (!base::FeatureList::IsEnabled(features::kBookmarkOpenAllConfirmation) ||
+      child_count < bookmarks::kNumBookmarkUrlsBeforePrompting) {
     DoOpenPromptConfirm(
         browser, std::move(url_and_ids), initial_disposition,
         bookmark_folder_node_id,
